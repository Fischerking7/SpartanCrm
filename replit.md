# SalesOps Pro - Replit Configuration

## Overview

SalesOps Pro is a full-stack sales operations, commissions tracking, and QuickBooks reconciliation platform built for scaling from small teams (5 reps) to mid-size organizations (50-200 reps). The application provides role-based access control (REP/MANAGER/ADMIN), commission calculations, payment tracking, chargeback management, and comprehensive audit logging.

## User Preferences

Preferred communication style: Simple, everyday language.

## System Architecture

### Tech Stack
- **Frontend**: React 18 with TypeScript, Vite bundler, TailwindCSS
- **Backend**: Node.js with Express, TypeScript
- **Database**: PostgreSQL with Drizzle ORM
- **Authentication**: JWT-based auth with bcrypt password hashing
- **UI Components**: shadcn/ui (Radix primitives with Tailwind styling)

### Project Structure
The application follows a monorepo structure with clear separation:
- `client/` - React frontend application
- `server/` - Express backend API
- `shared/` - Shared TypeScript types and Drizzle schema
- `migrations/` - Database migration files (generated by Drizzle Kit)

### Role-Based Access Control
Three user roles with strict data isolation:
- **REP**: Can only view/manage their own orders and commissions
- **MANAGER**: Can view/manage orders for reps assigned to their team
- **ADMIN**: Full system access including user management, rate cards, and accounting functions

### Core Business Definitions
- **Earned**: Orders where jobStatus=COMPLETED AND approvalStatus=APPROVED
- **Paid**: Orders with imported payment data from QuickBooks (paidDate exists)
- **Chargebacks**: Negative commission entries tied to invoiceNumber; original orders are never modified
- **Immutability**: After approval, key order fields are locked; corrections require formal Adjustments

### Database Schema
Key tables defined in `shared/schema.ts`:
- Users, Providers, Clients, Services, RateCards
- SalesOrders, Incentives, OverrideAgreements
- Chargebacks, Adjustments, PayRuns
- UnmatchedPayments, UnmatchedChargebacks, RateIssues (exception queues)
- AuditLogs, ExportBatches, Counters

### API Design
RESTful endpoints with JWT authentication middleware. All sensitive operations create audit log entries. Key endpoint patterns:
- `/api/auth/*` - Authentication endpoints
- `/api/orders` - Sales order CRUD with role-based filtering
- `/api/admin/*` - Admin-only operations (approvals, accounting, user management)
- `/api/dashboard/*` - Role-specific dashboard statistics

### Frontend Architecture
- React Query for server state management and caching
- Wouter for client-side routing
- React Hook Form with Zod validation
- Theme support (light/dark mode via CSS variables)
- Collapsible sidebar navigation with role-based menu items

## External Dependencies

### Database
- **PostgreSQL**: Primary data store, connection via DATABASE_URL environment variable
- **Drizzle ORM**: Type-safe database queries and migrations
- **Drizzle Kit**: Schema push and migration generation (`npm run db:push`)

### Authentication
- **jsonwebtoken**: JWT token generation and verification
- **bcryptjs**: Password hashing with salt rounds

### File Processing
- **csv-parse**: Parsing CSV imports for payments and chargebacks
- **csv-stringify**: Generating CSV exports for accounting
- **multer**: Handling file uploads

### UI Framework
- **@radix-ui/***: Accessible, unstyled UI primitives
- **tailwindcss**: Utility-first CSS framework
- **class-variance-authority**: Component variant management
- **lucide-react**: Icon library

### State Management
- **@tanstack/react-query**: Server state caching and synchronization
- **react-hook-form**: Form state management
- **zod**: Runtime type validation and schema definition

### Environment Variables Required
- `DATABASE_URL`: PostgreSQL connection string
- `SESSION_SECRET`: JWT signing secret (falls back to default in development)